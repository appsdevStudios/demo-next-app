'use client'
import React from 'react'

const AddToCard = () => {
  return (
    <div>
      <button className='btn btn-primary uppercase text-white' onClick={() => {console.log('Click')}}>Add To Card</button>
    </div>
  )
}

export default AddToCard
